// storage.rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Allow read access to ALL files in emulator only.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
